================GIT===============

git - это програмное обеспечение для контроля версий 

git init - инициализация нашего объекта т создается папка .git(в ней находятся вс необходимые файлы нашего )

 git add {file or .} - добавление файлов в рабочей папке в индекс(Индекс в Git - это спец промежуточная область в которой хранятся ихзменение файлов на пути от  рабочей дтректории)
git commint -m 'комментарий' - добавляет все файлы которые находятся в индексе во внутреннию БД и сохр их состояние на данный момент 


